{
  "name": "console-chatbot",
  "version": "0.0.1",
  "description": "Generative AI training application",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "npm run build && node dist/index.js",
    "dev": "npm run build && node --inspect dist/index.js",
    "postinstall": "npm run build"
  },
  "keywords": [
    "AI",
    "chatbot"
  ],
  "author": "AndriyS",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.14.2",
    "nodemon": "^3.1.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "@googleapis/customsearch": "^3.0.0",
    "@langchain/openai": "^0.1.2",
    "neo4j-driver": "^5.21.0"
  }
}
